main(){
  // TODO get the command-line arguments
  // TODO check if there is exactly two arguments
  // TODO parse them as integer and add them
  var a = 0;
  var b = 0;
  var result = add(a, b);
  print("$a + $b = $result");
}

int add(int a, int b){
  return a + b;
}